import * as React from 'react';
import { Helmet } from 'react-helmet';

const Home = () => {
  return (
    <React.Fragment>
      <Helmet>
        <title>Home page</title>
      </Helmet>
    </React.Fragment>
  );
};

export default Home;
